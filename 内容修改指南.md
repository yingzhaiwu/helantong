# 荷兰同城（NTC）· 内容快速修改指南

> 一句话原则：**90% 的内容改动只需要动一个文件 —— `assets/js/data.js`；改配色只动 `assets/css/style.css` 顶部的 `:root` 变量。**
> 本项目是纯静态站点，**没有构建步骤**：改完保存 → 浏览器刷新即可看到效果。

**内容现状（最近一次导入：2026-09-19，源文件 `招请1909.txt`）**

| 项目 | 数量 |
|---|---|
| 帖子总数 | **187 条**（原有 39 + 本次导入 148） |
| 分类 | 8 个（招聘 105 / 求职 19 / 租房 20 / 商铺 18 / 交友 6 / 优惠 4 / 装修 9 / 广告 6） |
| 城市 | 92 个（含「全荷兰」），本次新增 22 个荷兰小镇 |
| 源文件处理 | 175 行有效 → 去重 149 条唯一信息 → 生成 148 条（120 号一行含两家公司，拆成 2 条）；剔除 116 / 119 两条免税香烟 |

导入方法与校验要点见 **4.1 节**。

---

## 0. 先搞清楚：三层文件各管什么

| 层级 | 文件 | 管什么 | 改动频率 |
|---|---|---|---|
| 数据层 | `assets/js/data.js` | 站名、LOGO、分类、城市、帖子、广告条、广告位、价格区间 | ⭐⭐⭐ 最常改 |
| 样式层 | `assets/css/style.css` | 配色、字号、间距、圆角、响应式断点 | ⭐⭐ 偶尔改 |
| 页面层 | `*.html` | 页面结构、静态文案（关于我们、登录页说明等） | ⭐ 很少改 |
| 逻辑层 | `assets/js/*.js`（非 data.js） | 交互、筛选、表单字段 | 一般不用改 |
| 素材层 | `assets/img/` | 网站用到的图片（广告图等）。里面 `README.txt` 有说明 | ⭐ 传图时用 |

⚠️ 注意：你在网站上**自己发布的帖子**存在浏览器 localStorage（键名 `hh_posts`）里，不在 `data.js`。
要清掉测试数据：浏览器 F12 → Application → Local Storage → 删掉 `hh_posts` / `hh_user`，或直接换浏览器/无痕窗口。

⚠️ 同理，用管理员编辑器改的**广告位**存在 `hh_ad_slots`。要让所有访客都看到，必须点「导出配置」
把代码粘回 `data.js` 的 `AD_SLOTS`（见 5.5 节）。

---

## 1. 改站名 / 口号 / LOGO

**位置：** `assets/js/data.js` 第 5–26 行

```js
const SITE = {
  name: '荷兰同城',                 // ← 全站显示名（页头、页脚、Tab 标题后缀）
  en: 'NTC',                        // ← 英文缩写，只用于版权行 / Cookie 法律文案
  slogan: '在荷兰，找信息，上荷兰同城',   // ← 首页大标题下的标语
  desc: '荷兰华人同城信息平台 · 招聘 · 求职 · 租房 · 商铺 · 交友 · 优惠'  // ← 首页副标题
};

const LOGO_SVG = `<svg ...>`;      // ← 第 13 行，直接替换整段 SVG
```

> 📌 **LOGO 下方那行英文小字（现在显示 `HelanTC`）不读 `SITE.en`**，而是直接写在
> `assets/js/common.js` 里，共 **2 处**（页首 + 页脚），搜索 `logo-en` 即可找到：
> ```js
> <span class="logo-en">HelanTC</span>
> ```
> 想改品牌英文名，改这两处；`SITE.en` 是给版权行 `© 2026 荷兰同城（NTC）` 与
> Cookie 法律文案用的「法律简称」，两者可以不一样。

**换 LOGO 两种做法：**

- **改颜色**：只改第 16–18 行的三个 `stop-color`（现在是 `#F0A868 → #DF7A3A → #C25F60` 郁金香渐变）。
- **换图形**：把第 13–26 行整段替换成你的 SVG（保持 `class="logo-mark" viewBox="0 0 40 40"`，正方形 40×40 最稳）。

### 🎨 品牌字「荷兰同城 HelanTC」的字体与配色

这组字是**纯 CSS 做的**，没有用任何网络字体（站点不依赖外部 CDN），全部在
`assets/css/style.css` 第 **139–176 行**（搜 `.logo-cn` / `.logo-en`）。分三层：

| 层 | 元素 | 字体 / 字号 | 配色 | 手法 |
|---|---|---|---|---|
| 中文 | `.logo-cn` | 继承全站字体栈（PingFang SC / 微软雅黑 …），**19px / 字重 800 / 字距 1.2px** | 渐变 `#071F2A → #0C3443 → #10596B → #12858F → #17A0A8`（深海青 → 亮青，左深右亮） | `background-clip: text` + `-webkit-text-fill-color: transparent` 做**渐变字** |
| 英文 | `.logo-en` | 几何无衬线栈：**Bahnschrift → Avenir Next → Century Gothic → Segoe UI Variable Display → Segoe UI**，**11px / 字重 700 / 字距 2.6px** | 渐变 `#0A2C39 → #0E3B4A → #12707E`（比中文更暗一档，保证英文是配角） | 大写字距做 **wordmark** 感 |
| 细线 | `.logo-en::after` | — | 渐变 `#D98F4E → #12858F → #2FB6BE`（暖琥珀 → 亮青） | 2px 圆角短横线，压在英文下方 |

**为什么这么配：**

- 左侧 LOGO 是**暖橙渐变 + 白郁金香**，如果文字只用纯深色，标和字会像两块拼贴。
  细线的**左端用暖琥珀**、紧挨着标，等于把标的暖色「引」进文字区，再一路收进亮青 —— 三者成为一组。
- 中文渐变**左深右亮**，视觉重心落在「荷兰」上、由「城」收亮，有纵深且不呆板；
  四个色标把过渡摊平，避免最后一个字突然跳亮。
- 英文与中文**同向**（深→亮）但整段压在更暗的区间：既不会和中文抢视线，
  又不会因为方向相反导致视线来回跳。
- 字距：中文 1.2px 让方块字透气；英文 2.6px 是 wordmark 的典型处理，小字也能立得住。

**想自己调：**

| 想改什么 | 改哪里 |
|---|---|
| 中文大小 / 字重 / 字距 | `.logo-cn` 的 `font-size` / `font-weight` / `letter-spacing` |
| 中文渐变色 | `.logo-cn` 的 `linear-gradient(...)` 里的色值（保持「深 → 亮」顺序即可） |
| 英文用别的字体 | `.logo-en` 的 `font-family` 栈，**把想要的字体名放最前面**（找不到就自动往后fallback） |
| 英文大小 / 字距 | `.logo-en` 的 `font-size` / `letter-spacing`（改了字距要**同步改 `::after` 的 `right`**，否则细线对不齐字尾） |
| 不要那条细线 | 删掉 `.logo-en::after` 整段（或把 `height` 改成 `0`） |
| 细线颜色 / 粗细 | `.logo-en::after` 的 `background` / `height` |

> ⚠️ **两个坑**
> 1. **`-webkit-text-fill-color: transparent` 会被子元素继承**。所以 `.logo-cn` / `.logo-en` 里
>    **不要再嵌 `<span>` 加别的颜色**，否则会一起变透明。真要嵌套，给子元素加
>    `-webkit-text-fill-color: initial` 兜底（全站 `.section-title > span` 就是这么处理的）。
> 2. 页首高度固定 `--header-h: 60px`（`:root` 里）。把中文调到 **21px 以上**就要顺手把
>    `--header-h` 一起调大，否则品牌区会顶到页首上下边缘。改完记得在**窄屏**也看一眼
>    （`.header-inner` 是 `flex-wrap: wrap`，太高会换行）。

**别忘了同步这两处：**
1. 每个 `.html` 里的 `<title>...</title>`（浏览器标签页标题）
2. `assets/` 下的 favicon（如果有的话）

---

## 2. 改分类（增删改一个大类）

**位置：** `assets/js/data.js` 第 29–70 行 `CATEGORIES`

```js
{
  id: 'jobs',                  // 英文标识，全站唯一，不要随便改（涉及路由）
  name: '招聘信息',              // ← 显示名
  icon: '💼',                   // ← 图标，直接换 emoji
  color: '#B4692A',            // ← 分类主色（图标底色/描边）
  bg: '#FAF1E7',               // ← 分类浅色底
  en: 'Jobs',
  desc: '中餐厅 / 物流 / 零售 / 客服',   // ← 一句话说明
  subs: ['厨师后厨','餐厅服务','物流仓储', ...]  // ← 子分类，随便加减
}
```

**加一个新分类时，需要同步 4 处：**

1. `CATEGORIES` 里加一项（第 29 行起）
2. `PRICE_RANGES`（第 94 行）加对应价格筛选项，例如 `build: ['面议','€500 以下','€500-2000', ...]`
3. `assets/js/publish.js` 的 `FIELDS` 里加该分类的发布表单字段配置
4. 如果分类数变化导致首页宫格排版不好看 → 改 `style.css` 里 `.cat-grid` 的 `grid-template-columns`（现在是 4 列）

---

## 3. 改城市列表

**位置：** `assets/js/data.js` 第 118–132 行 `CITY_GROUPS`

```js
const CITY_GROUPS = [
  { g: '热门城市', list: ['阿姆斯特丹','鹿特丹','海牙','乌得勒支','埃因霍温'] },
  { g: '北荷兰省 Noord-Holland', list: ['哈勒姆','阿尔梅勒', ...] },
  ...
];
```

- **加城市**：往对应省份的 `list` 数组里加字符串即可，**重复的城市会自动去重**。
- **加省份**：复制一整行 `{ g: 'xxx', list: [...] }`。
- 第 135 行 `CITIES` 是自动拼出来的（`['全荷兰'] + 所有省份城市`），**不用手动改**。
- 城市列表是「按省份完整列举」的**地理浏览维度**，不要求每个城市都有帖子（当前 92 个城市里 62 个有帖，其余 30 个是留给后续内容的）。所以加了城市但暂时没内容，不算错。

> ⚠️ **别把字段名写错**：是 `g` 和 `list`，不是 `group` 和 `cities`。写错的话该省份整组城市都不会出现在筛选条里，而且不会报错。

---

## 4. 改帖子 / 加内容（最常用）

**位置：** `assets/js/data.js` 第 349–1861 行 `POSTS`，**当前共 187 条**

文件里已用注释分好了 8 段，直接搜索 `/* ===== xxx ===== */` 定位：

| 分类 | 注释所在行 | id 段 | 条数 |
|---|---|---|---|
| 招聘信息 | 350 | 1001–1105 | 105 |
| 求职信息 | 1192 | 2001–2019 | 19 |
| 租房信息 | 1346 | 3001–3020 | 20 |
| 商铺买卖 | 1508 | 4001–4018 | 18 |
| 生活交友 | 1654 | 5001–5006 | 6 |
| 优惠信息 | 1704 | 6001–6004 | 4 |
| 建筑装修 | 1738 | 7001–7009 | 9 |
| 其他广告 | 1812 | 8001–8006 | 6 |

> 行号会随内容增删变化，**以 `/* ===== 分类名 ===== */` 注释为准**，别死记数字。

**一条帖子的完整结构（照抄改值即可）：**

```js
{
  id: 1001,                 // 全站唯一，别重复
  cat: 'jobs',              // 对应 CATEGORIES 的 id
  sub: '厨师后厨',            // 必须是该分类 subs 里的一项
  title: '阿姆斯特丹市中心中餐厅诚聘川菜厨师',   // 标题
  city: '阿姆斯特丹',         // 必须是 CITIES 里的城市
  district: 'Centrum',      // 区域，可留 ''
  price: '€3000-3600/月',   // 价格 / 薪资文案
  emoji: '🍜',              // 封面占位图标
  urgent: false,            // true = 加急红标签
  tags: ['全职','包吃住'],    // 标签 chips，2–4 个最好
  author: 'Amstel中餐',      // 发布者
  mins: 26,                 // 多少分钟前发布（显示成"26分钟前"）
  views: 1862, likes: 74, comments: 22,   // 热度数字
  desc: '一句话摘要',         // 列表页显示
  hl: ['亮点1','亮点2'],      // 详情页"信息亮点"列表
  contact: { wechat: 'amstel_hr', phone: '+31 6 ...', email: '...' }
}
```

**批量操作小技巧：**
- 想让某条帖子不显示 → 在它前面加 `//` 注释掉，或把 `cat` 改成不存在的值（不推荐）。
- 想快速换一批同城数据 → 用编辑器的「查找替换」批量改 `city: '阿姆斯特丹'` → `city: '鹿特丹'`。
- 详情页评论是第 1864 行 `COMMENT_SEED`，按帖子 id 分组。

**id 编号规矩（加新帖时照这个来）：**

| 分类 | id 前缀 | 下一个可用 id |
|---|---|---|
| 招聘信息 | 1xxx | 1106 |
| 求职信息 | 2xxx | 2020 |
| 租房信息 | 3xxx | 3021 |
| 商铺买卖 | 4xxx | 4019 |
| 生活交友 | 5xxx | 5007 |
| 优惠信息 | 6xxx | 6005 |
| 建筑装修 | 7xxx | 7010 |
| 其他广告 | 8xxx | 8007 |

同一分类内 id **必须唯一**（详情页靠 `detail.html?id=xxxx` 定位，重了会串内容）。

### 4.1 从招请群文本批量导入（本次 148 条就是这么来的）

源文件 `招请1909.txt`（175 行）→ 去重后 149 条唯一信息 → 生成 148 条帖子。流程可复用：

1. **按行拆条**：一行一条，先做 `squeeze` —— 把「数字 空格/横杠 数字」中间的分隔符去掉，否则分段写的号码（`0644  888  579`）会漏识别。
2. **提电话**：`(?<!\d)(?:\+?31|0031)?0?6\d{8}(?!\d)`，**先去分隔符再匹配**。
3. **去重**：电话相同 **且** 内容指纹前 14 字互相包含 → 判为同一条（同一雇主连发多行）。仅电话相同、内容不同的**不能合并**（同一老板在不同城市开两家店是常事，本次有 4 组这种）。
4. **一行含多家** → 拆成多条（本次 120 号一行两家装修公司 → 拆 2 条）。
5. **分类**：`cat` + `sub` 必须是 `CATEGORIES` 里已有的；城市必须是 `CITIES` 里的；缺的城镇先补进 `CITY_GROUPS`。
6. **合规排除**：与站点「不违法」规则冲突的条目直接剔除（本次剔除 116 / 119 两条免税香烟）。
7. **落地后必须校验**（脚本见下）：总数、id 唯一、`cat`/`sub`/`city` 全在白名单内、`hl` 非空、电话格式可解析。

**校验脚本要点**（Node 跑，`data.js` 只定义顶层 `const`，可安全 `new Function` 求值）：

```js
const D = new Function(fs.readFileSync('assets/js/data.js','utf8') + '\n;return {POSTS,CATEGORIES,CITIES};')();
// 注意：const 声明不会挂到 window 上，浏览器里要用 eval('POSTS') 取，别写 window.POSTS
```

---

## 5. 改滚动广告条

**位置：** `assets/js/data.js` 第 150–159 行 `ADS_BAR`

```js
const ADS_BAR = [
  { tag: '推荐', text: '阿姆斯特丹东区精装单间 €780，可注册', href: 'detail.html?id=3001' },
  ...
];
```

- `tag`：左侧小标签文字（推荐 / 装修 / 招聘 / 优惠 / 广告位…）
- `text`：滚动正文
- `href`：点击跳转（帖子详情用 `detail.html?id=xxxx`，板块页用 `build.html` / `ads.html`，外链直接写 `https://...`）
- 条目少于 3 条时滚动会有空隙，建议保持 **6–10 条**。

**广告条不显示了？** 关闭状态存在 `sessionStorage` 的 `hh_adbar_off`，**关掉标签页重开就会恢复**。

---

## 5.5 改首页侧栏广告位（8 个招租位）

首页**第三列**「📣 广告位招租」区块（最右栏），共 **8 个位置**，默认全部空置显示「广告位招租」。
**只有管理员身份**登录后才会出现编辑入口（每个格子右上角的 ✎ 和管理员徽章）。

**位置：** `assets/js/data.js` 的 `AD_SLOTS`

```js
const ADMIN_ACCOUNTS = [
  { account: 'admin@ntc.nl', password: 'NTCadmin2026', name: '站长' }   // ← 管理员账号名单（含专用密码）
];
const AD_SLOTS = [
  { id: 'ad1', emoji: '', img: '', mode: 'card', title: '', text: '', link: '' },
  { id: 'ad2', emoji: '', img: '', mode: 'card', title: '', text: '', link: '' },
  ...
];
```

### 🔑 管理员账号（默认）

| 项目 | 值 |
|---|---|
| 登录账号 | `admin@ntc.nl` |
| 登录密码 | `NTCadmin2026` |
| 显示昵称 | 站长 |

在 `login.html` 登录页输入以上账号 + 密码即可进入管理员身份。**密码必须完全一致**，
写错了会提示「管理员密码不正确」且不会授予管理员权限；其他普通账号即使注册登录，
也拿不到任何广告位编辑入口。

要改账号或密码，只改 `data.js` 里 `ADMIN_ACCOUNTS` 数组即可（可放多条）：

```js
const ADMIN_ACCOUNTS = [
  { account: 'admin@ntc.nl', password: 'NTCadmin2026', name: '站长' },
  { account: 'boss@ntc.nl', password: 'AnotherPwd888', name: '运营' },
];
```

| 字段 | 说明 |
|---|---|
| `img` | **广告图片**。可写站内路径 `assets/img/ad1.jpg`、完整网址 `https://…`，或上传后自动生成的 `data:image/…` 内嵌图。留空则改用 `emoji` |
| `mode` | **显示方式**，只有两个值：`'card'` = 图文卡片（图片当左侧缩略图）｜`'cover'` = 整幅铺满（图片铺满整格、文字压在上面）。默认 `'card'` |
| `emoji` | 图标，一个 emoji，如 `🍜`（没有图片时显示；留空默认 📣） |
| `title` | 广告标题，**留空即为「招租中」** |
| `text` | 补充说明一行，可留空 |
| `link` | 点击跳转，可写 `https://…` 或站内 `list.html?cat=deal`（留空则不可点） |
| `expiresAt` | **过期时间戳**（毫秒）。`0` 表示永不过期。管理员在页面上编辑时可设「广告有效天数」，系统自动计算。到期后普通访客看不到，管理员仍能看到灰显状态（带「已过期」角标），每天首次加载时自动清理并记入操作日志 |

> 「已投放」的判定：`title`、`img`、`emoji`、`text` **任意一个非空**，该格就不再显示「招租中」。
> 所以只传一张图、不写标题，也能当成一条图片广告。

填好的例子：

```js
/* 图文卡片：左边一张缩略图，右边标题 + 说明 */
{ id: 'ad1', emoji: '', img: 'assets/img/ad1.jpg', mode: 'card',
  title: '鹿特丹中餐配送', text: '满 €30 全城免运费', link: 'https://example.com' },

/* 整幅铺满：整张图铺满格子，标题与说明压在图片下缘（建议用横图，宽度 600px 以上） */
{ id: 'ad2', emoji: '', img: 'assets/img/ad2.jpg', mode: 'cover',
  title: '海牙装修队 · 全荷上门', text: '免费量房报价', link: 'build.html' },

/* 不用图片，只用 emoji + 文字 */
{ id: 'ad3', emoji: '🍜', img: '', mode: 'card',
  title: '川菜馆开业', text: '首单 8 折', link: 'list.html?cat=deal' },
```

### 🖼 上传图片（不用懂代码）

用管理员登录 → 首页侧栏点某个格子的 **✎** → 弹窗里左栏第一项就是图片区：

| 操作 | 怎么做 |
|---|---|
| 上传 | **点一下虚线框**，或**把图片从文件夹直接拖进去** |
| 换一张 | 点「更换」 |
| 删掉 | 点「✕ 删除」 |
| 不想上传、只想用网上现成的图 | 在下面的输入框直接粘贴图片地址（`assets/img/ad1.jpg` 或 `https://…`） |
| 想把上传的图存成文件 | 点「⬇ 下载」，图片会存到你电脑，再放进 `assets/img/`，然后把 `img` 改成该路径 |

**图片会被自动压缩。** 站点没有服务器，上传的图只能转成 `data:image/…` 存在浏览器里，
所以脚本会先处理一遍再存：

- 最长边超过 **1200px** 就等比缩小；
- 透明 PNG 自动垫白底（避免变黑）；
- 优先输出 **WebP**（比 JPEG 通常小 30%），浏览器不支持时退回 JPEG；
- 结果超过 **380KB** 就逐步降质量（0.82 → 最低 0.5）。
- 单张原图上限 **20MB**，超过会提示先裁剪。

上传后虚线框下面会显示「已压缩到 1280×720 · 约 96 KB」，一眼能看到压缩结果。

> ⚠️ **base64 内嵌图的取舍**：上传的图变成一长串字符存在 `localStorage`（约 5MB 配额）里，
> 好处是**零配置、立刻能看**；坏处是数据变长、且**只有你这台电脑能看到**。
> 想让它对所有访客生效，点「⬇ 下载」把图存到 `assets/img/`，`img` 写路径 —— 这样最干净。
> 点「导出配置」时，若还有 base64 内嵌图，弹窗会橙色提醒你哪几位需要处理。

### 🎛 两种显示方式怎么选

| 显示方式 | 效果 | 适合 |
|---|---|---|
| **图文卡片** `card` | 图片作左侧缩略图（66×54），右侧标题 + 说明 | 想和旁边的信息卡风格统一、多放几条 |
| **整幅铺满** `cover` | 图片铺满整格，底部压一层深色渐变 + 标题 + 说明 | 只有一两条重点广告，想抢眼 |

切换方式：编辑器里点「显示方式」的**图文卡片 / 整幅铺满**。
没有图片时这个开关是灰的，点了会提示「先上传一张广告图片」。
编辑过程中右栏有**实时预览**，改标题、换图、切模式都会立刻反映，不用保存就能看效果。

### 🧯 图片地址写错了会怎样（有兜底，不会留空白）

`img` 填了一个不存在的路径、或者外链挂了，浏览器加载失败时脚本会自动兜底：

- 把那块破图**移除**，广告格改成**浅斜纹底 + 纯文字**（标题、说明照常显示），
  而不是留一块刺眼的空白；
- 右上角标签从「广告」变成红色的「**广告 · 图未加载**」；
- 鼠标停在格子上能看到失败原因（悬停提示里带出错的地址前 80 个字符）。

所以万一图挂了，访客至少还能看到你的文字，你自己也能一眼发现是哪一格出的问题。

> 💡 **小技巧**：管理员身份下，**空位整格都能点**（不用瞄准右上角那个小 ✎ 按钮），
> 点哪儿都能打开编辑器。已投放的格子则保持「点哪儿都跟广告链接走」，两者不打架。

**两种改法：**

1. **页面上改（推荐给日常运营）** —— 用管理员账号登录 → 首页侧栏出现 ✎ → 点开填写 → 保存。
   结果存在本机浏览器，可即时预览。改完点底部「**导出配置**」，把生成的代码复制回 `data.js` 的 `AD_SLOTS` 并保存，**全部访客**才会看到。
2. **直接改文件** —— 按上表填 `data.js` 的 `AD_SLOTS`，保存刷新即可，对所有人生效。

### ⏳ 广告有效期限（自动下架）

管理员在编辑器里可以为每条广告设置**有效天数**（1–365 天，默认 30 天）：

- 保存时系统**自动计算到期时间**（`expiresAt = 当前时间 + 天数 × 86400000 毫秒`）；
- **普通访客**：到期后该格自动恢复成「广告位招租」空位，完全看不到过期内容；
- **管理员**：到期后仍能看到该广告，但会变成**灰显 + 红色「已过期」角标**，链接不可点，方便你知道哪条该续费或撤下；
- **每天首次打开首页时**，系统自动扫描所有广告位，把过期的从 `hh_ad_slots` 里**物理删除**，同时往 `hh_ad_log` 写一条 `expire` 操作日志（含时间、广告位 ID、标题、操作人）；
- 手动点「清除此位」「全部重置」也会记日志（`clear` / `reset`），方便审计；
- 导出配置时，`expiresAt` 字段会一并输出，粘回 `data.js` 即可永久生效。

> 直接改文件时，`expiresAt` 填 `0` 表示永不过期；填具体时间戳则到点自动下架。

**版式：** 8 个位置**竖向排成一列**，并自动与左/中两栏**等高**（三栏顶边、底边齐平）。
这是靠 `.layout-3col { align-items: stretch }` + 卡内 `display:flex` + `.ad-rent-grid { flex: 1 1 auto }`
实现的：只要左侧信息流条数/高度变化，8 格会自动重新均分，不用手调。
≤1100px 起三列解散（同城最新整宽在上、热帖与广告位并排在下），≤720px 再改为单列堆叠、取消等高。

**每格内容与右侧按钮（按身份分三态）：**

| 状态 | 左侧 | 中间 | 右侧 |
|---|---|---|---|
| 空位 · 访客 | `AD 01` 编号徽章 | 广告位招租 / 标准横幅位 / 支持图片·文字链 | 虚线胶囊「洽谈 ›」→ `about.html#contact` |
| 空位 · 管理员 | `AD 01` 编号徽章 | 广告位招租 / 点击右侧按钮投放 | 实心胶囊「✎ 投放」 |
| 已投放（卡片）· 访客 | 图片缩略图，没有图则 emoji | 标题 + 说明 | 无（右上角「广告」标） |
| 已投放（卡片）· 管理员 | 图片缩略图，没有图则 emoji | 标题 + 说明 | 圆形「✎」 |
| 已投放（铺满）· 任意身份 | 图片铺满整格，文字压在下缘 | — | 管理员另有右下角圆形「✎」 |

**相关样式位置：** `assets/css/style.css`

- 搜索「侧栏广告位招租」：`.ad-rent-grid`（一列网格 + 均分）· `.ad-rent-slot`（单个格子）·
  `.ars-no` / `.ars-body` / `.ars-tag` / `.ars-cta`（格内四块）· `.ars-img`（卡片左侧缩略图）·
  `.is-cover` / `.ars-bg` / `.ars-cover-body`（整幅铺满三件套）· `.is-noimg`（图片加载失败兜底）·
  `.ad-rent-slot[data-ad-edit]`（空位整格可点）· `.ad-rent-foot`（底部说明）。
- 搜索「广告位编辑器」：`.modal-lg`（大号弹窗 880px）· `.ade`（左表单 + 右预览双栏）·
  `.ade-drop`（上传/拖拽区，含 `.is-over` 拖入态）· `.ade-btn`（更换/下载/删除三个小按钮）·
  `.ade-seg`（图文卡片 / 整幅铺满 分段开关）· `.ade-quick`（常用链接）· `.ade-tip`（灰色小提示）。

**相关脚本位置：** `assets/js/common.js`

| 函数 | 作用 |
|---|---|
| `adSlotHtml(s, opts)` | 单个广告格的 HTML（空位 / 卡片 / 铺满三分支）。编辑器右栏的实时预览也复用这一份，保证所见即所得 |
| `renderAdSlots(box)` | 把 8 格渲染进首页 |
| `openAdSlotEditor(id)` | 弹出编辑器（上传、文字、链接、显示方式、实时预览） |
| `readAdImage(file)` | 图片纯前端压缩，返回 `{ dataUrl, w, h, kb }` |
| `exportAdSlots()` | 导出可粘回 `data.js` 的 `AD_SLOTS` 代码 |

---

## 5.6 首页三栏排版规则（广告位招租 / 本周热帖 / 同城最新）

首页的 `.layout` 额外挂一个 `layout-3col` 类，变成**三列等宽并排**，桌面端从左到右依次是
「广告位招租 → 本周热帖 → 同城最新」，三栏顶部与底部严格齐平：

```
┌───────────────────┬───────────────────┬───────────────────┐
│ ▌📣广告位招租 剩余8位│ ▌🔥本周热帖  更多 ›│ ▌同城最新 [最新…]  │
│ ┌───────────────┐ │ ┌───────────────┐ │ ┌───────────────┐ │
│ │ AD 01 … 洽谈 › │ │ │ 榜单 ×14       │ │ │ 信息卡 ×8      │ │
│ │ AD 02 … 洽谈 › │ │ │               │ │ │  （紧凑版）     │ │
│ │ …×8           │ │ │               │ │ │               │ │
│ └───────────────┘ │ └───────────────┘ │ └───────────────┘ │
│   共 8 个位置…     │                   │     [加载更多]     │
└───────────────────┴───────────────────┴───────────────────┘
   minmax(0, 1fr)      minmax(0, 1fr)      minmax(0, 1fr)
       ≈ 404px             ≈ 404px             ≈ 404px
```

**HTML 结构（`index.html`）** —— 三块都是 `.layout.layout-3col` 的**直接子元素**，
**DOM 顺序 = 桌面端从左到右的顺序**：

```html
<div class="layout layout-3col">
  <section class="side-card mod ad-rent-card">…</section><!-- ① 广告位招租 -->
  <section class="side-card mod hot-card">…</section>    <!-- ② 本周热帖 -->
  <div class="feed-col">…同城最新…</div>                 <!-- ③ 同城最新 -->
</div>
```

> ⚠️ **窄屏要改顺序，就改这里的 DOM 顺序 + `≤1100px` 的 `order`。**
> 因为移动端主内容必须是帖子列表，`≤1100px` 那段媒体查询里用 `order` 把三块
> 重排成了「同城最新 → 本周热帖 → 广告位招租」（见下面「各断点表现」）。

**三条对齐线（改版型时不要破坏）：**

1. **标题行齐平** —— 三栏都用 `.mod-head`（`padding-top:16px` + `min-height:60px` + `margin-bottom:14px`），
   标题统一用 `.section-title`（18px/800 + 深青→亮青渐变字 + 左侧 5×20px 渐变色条），
   右侧附件（tabs / 更多 › / 剩余 N 位）统一用 `.mod-acts`。所以三栏的标题、内容起始线在同一水平线上。
2. **三栏等宽** —— 宽度只由 `.layout.layout-3col` 一条规则决定（见下）。
3. **顶部 + 底部齐平** —— `.layout-3col` 用 `align-items: stretch`；中/右两卡内部再
   `display:flex; flex-direction:column`，让内容区（`.rank-list` / `.ad-rent-grid`）以 `flex:1 1 auto`
   吃掉剩余高度，榜单行与广告格再用 `flex:1 1 0` 等分，于是**三栏底部自动齐平**（1440/1280 实测差 0px）。

**改宽度只需动一处：**

```css
/* 当前：三栏等宽 */
.layout.layout-3col { grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 18px; align-items: stretch; }

/* 若想改回「主栏宽 + 两个窄栏」，把上面那行换成下面这行即可（其它都不用动）：
   grid-template-columns: minmax(0, 1fr) 256px 308px;  */
```

- `minmax(0, 1fr)` **不能改成 `1fr`** —— `1fr` 等价于 `minmax(auto, 1fr)`，会被不可收缩的长标题撑破横向。
- 这条规则**只作用于首页**（用 `.layout-3col` 提高权重）；其它页面（列表页 / 个人中心等）仍是
  `.layout` 的两列 `minmax(0, 1fr) 320px`，互不影响。

**为了让「等宽」好看，做了三处内容配平**（改宽度或改条数时一并留意）：

| 位置 | 处理 | 为什么 |
|---|---|---|
| 同城最新（右栏） | `@media (min-width: 1101px)` 里的 `.layout-3col .feed-col .post-card` 紧凑版：缩略图 116→92px、字号收一档、标签行 `max-height: 46px` | 等宽后每栏只剩约 404px，标准卡片会涨到 250px+，把另外两栏一起拉得过于空旷 |
| 本周热帖（中栏） | `home.js` 里 `slice(0, 14)`（原 10 条） | 行高由 `flex: 1 1 0` 等分，条数多则每行矮，避免一行标题就占 200px |
| 广告位招租（左栏） | 空位改成三行文案（`.ars-empty-t` + `.ars-empty-s` + `.ars-spec` 规格行），编号徽章 52×42 → 58×50 两行（`<i>AD</i>01`） | 格子被拉高后内容不至于空旷 |

**各断点表现：**

| 宽度 | 顺序（上→下 / 左→右） | 布局 |
|---|---|---|
| ≥1101px | 广告位招租 → 本周热帖 → 同城最新 | **三列等宽并排**，三栏顶/底齐平；同城最新启用紧凑卡片 |
| 721–1100px | **同城最新** → 本周热帖 → 广告位招租 | 同城最新**整宽在上**（`grid-column: 1 / -1`），热帖与广告位**并排在下**、两卡等高 |
| ≤720px | 同城最新 → 本周热帖 → 广告位招租 | **单列堆叠**；标题行压到 56px（字号 17px）；榜单行恢复自然高度（`flex: 0 0 auto`） |
| ≤420px | — | 分类宫格降为 3 列 |

**窄屏换序靠 `order`**（写在 `≤1100px` 媒体查询里）：

```css
.layout.layout-3col > .feed-col    { grid-column: 1 / -1; order: -2; }  /* 提到最前 */
.layout.layout-3col > .hot-card    { order: -1; }
.layout.layout-3col > .ad-rent-card{ order:  0; }                       /* 垫底 */
```

> ⚠️ **三个坑：**
> 1. `.rank-list li` 在 ≥721px 是 `flex: 1 1 0`（等分高度）。热帖条数一改（比如 14 → 8），
>    行高会整体变高，需同步改 `home.js` 里的 `slice(0, 14)`。
> 2. 榜单标题 `.rank-title` 必须保留 `-webkit-line-clamp: 2` + `overflow: hidden`，
>    否则超长标题会把中栏撑宽 → 三栏不再等宽。
> 3. 紧凑卡片包在 `@media (min-width: 1101px)` 里。**若哪天改了「三列解散」的断点，
>    这里要同步改**，否则 ≤1100px 整宽状态下也会套用紧凑卡片（显得又扁又空）。

**关于栏目标题的美化（`同城最新` / `本周热帖` / `广告位招租` 共用一套）：**

标题样式集中在 `style.css` 的 `.section-title`（约第 221 行），要点：

```css
.section-title {
  font-size: 18px; font-weight: 800; letter-spacing: .4px;
  background-image: linear-gradient(96deg, var(--brand-800), var(--brand) 42%, var(--teal) 96%);
  -webkit-background-clip: text; background-clip: text;
  -webkit-text-fill-color: transparent;     /* ← 靠这两行做出渐变字 */
}
.section-title::before { /* 左侧 5×20px 三段渐变"书签条" + 内高光 + 外发光 */ }
```

> 🔴 **改标题时最容易踩的坑：`-webkit-text-fill-color: transparent` 会被子元素继承。**
> 所以标题里**任何带文字的子元素**（emoji、`（选填）` 这类小字）都会变成透明而「消失」。
> 项目里已经用一条兜底规则解决：
> ```css
> .section-title > span { -webkit-text-fill-color: initial; }  /* initial = currentColor */
> .section-title > .st-emoji { color: initial; }               /* emoji 交给彩色字体渲染 */
> ```
> **你自己往标题里塞东西时，记得用 `<span>` 包一层**，例如：
> ```html
> <div class="section-title"><span class="st-emoji">📣</span>广告位招租</div>
> ```

---

## 6. 改配色 / 整体风格

**位置：** `assets/css/style.css` 最顶部 `:root { ... }`（约第 1–55 行）

```css
:root {
  --brand: #0E3B4A;       /* 主色调：按钮/导航/链接 —— 改这里整站变色 */
  --brand-600: #0A2C39;   /* hover 深一档 */
  --brand-700: #071F2A;   /* 最深，标题/强调 */
  --brand-800: #04161E;   /* 极深，深色底 */
  --brand-50: #E6F1F4;    /* 主色最浅（背景块） */
  --brand-100: #C1DBE2;   /* 主色浅（边框/分隔） */
  --brand-200: #98C1CB;   /* 主色浅（hover 描边） */

  --teal: #12858F;        /* 时尚点缀：渐变高光 / 焦点圈 */
  --teal-50: #E2F1F3;

  --accent: #BC6528;      /* 点缀色：价格数字专用 */
  --accent-50: #FBEEE1;
  --accent-100: #F0D6BC;

  --ink: #0E1A21;         /* 正文主文字色 */
  --ink-2: #3D5160;       /* 次要文字 */
  --ink-3: #71858F;       /* 辅助说明 */
  --line: #DAE5E8;        /* 分割线 */
  --line-2: #BFD1D6;      /* 边框 */
  --bg: #EEF4F6;          /* 页面底色 */
  --card: #fff;           /* 卡片底色 */
  --card-2: #F7FBFC;      /* 次级卡片底色 */

  --grad-brand: linear-gradient(135deg, #1B7F8C 0%, #0E3B4A 52%, #071F2A 100%);
  --grad-brand-h: linear-gradient(135deg, #0E3B4A 0%, #071F2A 100%);   /* hover 态 */
  --grad-soft: linear-gradient(160deg, #FCFEFE 0%, #F0F6F8 58%, #E4EEF1 100%);
  --grad-accent: linear-gradient(135deg, #E09A46 0%, #BC6528 55%, #96501D 100%);

  --radius: 16px;         /* 全局圆角 */
  --radius-sm: 12px;
  --radius-lg: 22px;
  --shadow-1: ...;        /* 三级阴影（带主色调） */
  --shadow-2: ...;
  --shadow-3: ...;
  --shadow-brand: 0 8px 22px rgba(14, 59, 74, .26);   /* 主色投影 */
  --ring: 0 0 0 3px rgba(18, 133, 143, .18);          /* 输入框焦点圈 */
}
```

**常见需求对照：**

| 想改成… | 改哪个变量 |
|---|---|
| 换个品牌色（比如改成绿色/蓝色） | `--brand` + `--brand-600` + `--brand-700` + `--brand-50` + `--brand-100` + `--grad-brand` 一起改 |
| 想换「时尚高光色」 | `--teal`（渐变起点、焦点圈、Hero 光晕都跟着变） |
| 页面太白，想更暖/更冷 | `--bg`、`--card`、`--card-2` |
| 价格橙色太跳 | `--accent` |
| 字太小/太大 | 搜 `font-size`，或改 `html { font-size }` |
| 想更圆润/更方正 | `--radius` / `--radius-sm` / `--radius-lg` |
| 卡片阴影太重 | `--shadow-1` ~ `--shadow-3` |
| 浏览器地址栏配色（手机端） | 各 html 第 7–8 行 `<meta name="theme-color" content="#0E3B4A">` |

改完保存，刷新页面即可 —— **不用重启任何服务**。

> ⚠️ 改主色时，除了 `style.css`，还有 3 处需要同步，否则会出现「新旧两种颜色」：
> 1. 各 html 的 `theme-color`（手机浏览器状态栏颜色）
> 2. `assets/js/common.js` 的 `AVATAR_COLORS`（默认头像色板）
> 3. `assets/js/data.js` 的 `CATEGORIES` 里各分类的 `color` / `bg`

### 6.1 移动端适配规则（改版型时看这里）

移动端样式集中在 `style.css` 末尾的 **`@media (max-width: 720px)`** 与 **`@media (max-width: 420px)`** 两段：

| 断点 | 生效范围 | 主要变化 |
|---|---|---|
| `≤1100px` | 平板横屏 | 宫格 4→3 列，页脚 6→3 列；**首页三列解散 →「同城最新整宽在上 + 热帖/广告位并排在下」，并用 `order` 把顺序重排成 同城最新→本周热帖→广告位招租** |
| `≤960px` | 平板 | 其它页面的主内容与侧栏改为上下单列（`.side-col` 变 2 列）；首页三列**不受影响**，仍保持「整宽 + 并排」直到 ≤720px |
| `≤860px` | 小屏平板 | 搜索框独占一行，城市选择器隐藏 |
| `≤720px` | **手机主断点** | 底部导航出现；触控目标放大到 ≥40px；信息卡图 88px；表单单列；筛选区改「标签 + 横向滑动胶囊」；**首页三栏彻底改单列堆叠、榜单行恢复自然高度、标题字号 17px** |
| `≤420px` | 小屏手机 | 分类宫格保持 3 列并收紧间距；发布页分类卡 2 列；信息卡改为大图上下结构 |

弹窗（`.modal`）另有一条规则：`≤720px` 时弹窗内边距收到 18px；**广告位编辑器**的双栏
（`.ade` 左表单 + 右预览）在 `≤720px` 改为上下单列，上传区的三个小按钮（更换/下载/删除）
换成横向一行；`≤1000px` 时这三个按钮也会提前换行，避免在窄栏里被挤扁。

另有 `@media (hover: none)` 一段，用于在触屏设备上关闭 hover 位移效果（避免点完还「粘住」）。

> ⚠️ **改布局时的头号坑：横向溢出（手机上能左右拖动）**
> 网格里写 `1fr` 等于 `minmax(auto, 1fr)`，如果格子里的内容**不能收缩**
> （长英文单词、`white-space: nowrap` 的标题、超长链接），整个网格会被撑宽，页面就溢出。
> 本项目已统一改成 `minmax(0, 1fr)` 并给相关元素补 `min-width: 0`：
> `.layout` / `.layout-3col` / `.post-card` / `.post-list` / `.side-col` / `.side-card` / `.ad-rent-grid` / `.rank-list a`。
> 首页三列另外还给 `.rank-title` 加了 `-webkit-line-clamp: 2`，用来兜住超长热帖标题。
> **你新增任何 flex / grid 容器时，请照抄这个写法。**
> 自检方法：浏览器控制台执行
> `document.documentElement.scrollWidth === document.documentElement.clientWidth`
> 返回 `true` 即无溢出（分别在 1440 / 768 / 390 宽度下各测一次）。

---

## 6.5 改积分规则 / 邀请奖励

**位置：** `assets/js/data.js` → `POINTS_RULES`（约第 180 行）与 `INVITE`（约第 191 行）

```js
const POINTS_RULES = { register:100, publish:100, invite:100, scrollPerDay:25, signIn:20 };
const INVITE = { bonus:100, title:'邀请好友，双方各得 100 积分', desc:'…', steps:[…3 条…] };
```

改这两个对象，以下位置**会自动同步**，不用逐个页面改：
首页邀请横幅（`#invBonus/#invScroll`）、个人中心积分页的规则表与按钮文案、发布页费用账单、邀请浮层里的说明与步骤。

**邀请入口都在哪（共 3 处）：**

| 位置 | 文件 | 说明 |
|---|---|---|
| 首页邀请横幅（分类宫格下方） | `index.html` 内 `.invite-strip` | 「立即邀请好友」+「每日签到 +20 积分」按钮，未登录也能看到并点击 |
| 个人中心 → ⭐ 积分管理 | `assets/js/user.js` `renderPoints()`（`#inviteBtn` / `#inviteBtn2`） | 含邀请记录与签到 |
| 发布页 / 帮助规则页 侧栏按钮 | `publish.html`、`about.html`，带 `data-invite` | 一键弹出邀请浮层 |

> 注：头部导航栏原来的「👥 邀请」按钮已移除（邀请入口改由首页邀请横幅承担）；首页邀请横幅的「每日签到」按钮逻辑在 `assets/js/home.js`（调用 `Store.signIn()`）。

> **万能用法：** 任何按钮加上 `data-invite` 属性，就会自动弹出邀请浮层（逻辑在 `common.js` 的 `openInviteModal()`）。

**未登录也能演示：** 邀请浮层与积分页都支持「用演示账号预览」（`useDemoAccount()`），一键写入演示会员并赠送注册积分，预览环境不会再出现空白页。

---

## 6.6 改 Cookie 条款与同意设置（GDPR / AVG）

**位置：** `assets/js/data.js` 搜索 `const COOKIE = {`（约第 200 行起），**所有文案、分类、政策正文都在这里**，改完两页政策与首访弹窗自动同步。

```js
const COOKIE = {
  version: '2026-09-18',   // 条款版本：改动后所有用户会被重新征求同意
  maxAgeDays: 180,         // 同意有效期，到期重新征求
  controller: '荷兰同城 NTC（Netherlands Tongcheng）',  // 数据控制者
  contact: 'privacy@ntc.nl',
  banner: { title: '我们使用 Cookie', text: '…' },       // 首访弹窗文案（打开站点即居中弹出）
  cats: [ /* 4 类：necessary / preference / analytics / marketing */ ],
  policy: [ /* Cookie 政策页正文，按 { h:'小标题', p:['段落',…] } 增删 */ ],
  privacy: [ /* 隐私政策页正文，格式同上 */ ]
};
```

**每个类别可改的字段：** `name`（名称）、`icon`、`required`（true = 必需且不可关闭）、`desc`（用途说明）、`items`（实际存放的 localStorage 键）、`retain`（保存期限）。

**真正的开关在这里：** `COOKIE_KEY_CAT`（同文件内）把每个 localStorage 键归类；未列出的键按「必要」处理。

```js
hh_user / hh_posts / hh_points …  → necessary   // 登录、发布、积分，永远可写
hh_city / hh_favs / hh_drafts …   → preference  // 未同意则不写入
hh_views / hh_likes               → analytics   // 未同意则不写入
```

> 逻辑由 `common.js` 的 `Cookie.can(类别)` 控制：`LS.get / LS.set` 会先检查该键所属类别是否已同意，未同意则**不写入**并提示一次。所以只要改 `COOKIE_KEY_CAT`，无需改业务代码。

**同意入口共 5 处（都是同一个浮层 `openCookieSettings()`）：**

| 入口 | 位置 |
|---|---|
| 首访居中弹窗（打开站点即弹出） | 自动出现（`renderCookieGate()`），遮罩 + 锁定滚动 + 无关闭按钮，含 ✓ 全部接受 / 仅必要 Cookie / ⚙️ 自定义设置；点遮罩只抖动提示不会关闭 |
| 右下角常驻 🍪 悬浮按钮（已移除） | 设置入口改到页脚「⚙️ Cookie 设置」、cookie.html、privacy.html、登录页 |
| 页脚「⚙️ Cookie 设置」与同意状态 | `common.js` `renderFooter()`，「法律与隐私」栏内显示当前同意状态（已移除重复的 footer-legal 行，页脚只此一处） |
| Cookie 政策页 `cookie.html` | 状态卡片 + 全部接受 / 仅必要 / 自定义 / 撤回同意 |
| 隐私政策页 `privacy.html`、登录页、个人中心「个人资料」 | 均有设置按钮 |

**上线前建议替换的 3 处：** ① `COOKIE.controller` 改成你的法律主体名称；② `COOKIE.contact` 改成真实隐私邮箱；③ 若接入第三方统计/广告脚本，在其加载前加 `if (Cookie.can('analytics')) { … }` 判断。

---

## 7. 改页面静态文案

| 想改的内容 | 文件 |
|---|---|
| 首页大标题 / 副标题 / 搜索框提示 | `index.html`（少量） + `data.js` 的 `SITE` |
| 荷兰风车插画（仅首页 Hero） | `data.js` 的 `WINDMILL_SVG`（L27 附近，纯 SVG 可整段替换）；转速改 `style.css` 的 `.wm-blades`（20s，数值越大越慢）；尺寸改 `.hero-wm`。注：邀请好友条的风车已移除，页脚无风车 |
| 滚动广告条速度 | `common.js` 的 `ADS_BAR.length * 11`（现 ≈ 88s，越大越慢）；首页横幅广告位在 `style.css` 的 `.ad-slot-track` 44s |
| 关于我们 / 联系方式 / 规则说明 | `about.html` |
| 登录页文案 | `login.html` |
| 建筑装修页的服务 / 流程 / 案例 | `data.js` 的 `BUILD_SERVICES`(107) `BUILD_STEPS`(119) `BUILD_CASES`(127) |
| 会员积分规则（注册 / 发布 / 邀请 / 签到 / 滚动） | `data.js` 的 `POINTS_RULES`（约 L181） |
| 邀请好友文案与奖励 | `data.js` 的 `INVITE`（约 L191）；链接与邀请码生成在 `common.js` 的 `Store.inviteLink()` / `inviteCode()` |
| 两种信息类型（普通 / 滚动）的名称与卖点 | `data.js` 的 `POST_KINDS`（约 L186） |
| 积分充值套餐 | `data.js` 的 `POINT_PACKAGES`（约 L205） |
| 广告页推广方式卡片 | 由 `POST_KINDS` 驱动（`ads.js`），不再有「站内广告位 / 预订」 |
| 页头 / 页脚 / 底部导航 | `assets/js/common.js` 的 `renderHeader()` / `renderFooter()` / `renderTabbar()` |
| 发布页表单字段（① 选择分类 + ② 标题 / 城市 / 详细描述 / 图片 + ③ 选填：滚动信息开关） | `publish.html`；`assets/js/publish.js` 的 `FIELDS`（分类专属字段）已停用不渲染；滚动信息由 `#f_scroll` 复选框控制（默认「普通」，勾选后按 25 积分 / 天）；发布表单已不再单独收集联系方式 |
| Cookie 条款 / 分类 / 首访弹窗文案 | `data.js` 的 `COOKIE`（含 `policy` 与 `privacy` 两篇正文），详见「6.6」 |
| Cookie 政策页 / 隐私政策页 | `cookie.html`、`privacy.html`（正文由 `data.js` 渲染，页面只放框架） |
| 哪个存储键属于哪类 Cookie | `data.js` 的 `COOKIE_KEY_CAT` |

---

## 8. 标准操作流程

```
1. 打开 helan-tongcheng/assets/js/data.js（或 style.css）
2. 按上面表格定位到对应行号
3. 改值 → 保存（Ctrl+S）
4. 回到浏览器，硬刷新：Ctrl+F5（避免缓存）
5. 没生效？F12 看 Console 有没有红色报错（多半是漏了逗号或引号）
```

**关于图片：** 发布页和广告位编辑器选图后都会**自动压缩**（最长边 1200px、优先 WebP、超标再降质量/降分辨率），
所以手机拍的大图可以直接传，不用自己先处理。压缩参数在 `assets/js/common.js` 的 `compressImage(file, opts)`，
`readPostImage`（帖子，240KB）与 `readAdImage`（广告位，380KB）是它的两个薄封装，要调就改这两个数字。

**本地预览地址：** 如果之前的预览服务还在，用 http://127.0.0.1:8199 ；
重新启动的话，在项目根目录执行：

```bash
cd "helan-tongcheng"
python -m http.server 8199
```

然后浏览器打开 `http://127.0.0.1:8199/index.html`。

> 直接双击 `index.html` 用 `file://` 打开也能看，但**发布/收藏等 localStorage 功能会失效**（opaque origin 限制），建议起本地服务。

---

## 9. 常见坑

| 现象 | 原因 / 解法 |
|---|---|
| 改了没反应 | 浏览器缓存 → Ctrl+F5；或改错文件（确认是 `helan-tongcheng/` 目录下那份） |
| 页面全白 / 报错 | `data.js` 里漏了逗号、多了末尾逗号、引号没闭合 → 看 F12 Console 的行号 |
| 新加的城市不显示 | 城市要写进 `CITY_GROUPS`，`CITIES` 是自动生成的。注意字段名是 **`g` 和 `list`**（不是 `group` / `cities`），写错整组城市都进不来且不报错 |
| 新分类点进去是空的 | 忘了在 `PRICE_RANGES` 和 `publish.js` 的 `FIELDS` 里补配置 |
| 帖子不显示 | `cat` 值拼错，或 `sub` 不在该分类 `subs` 里 |
| 侧栏榜单里文字**互相重叠** | 侧栏列表 `.rank-list li` 用 `flex:1 1 0`（有富余空间时等分拉伸，首页排行榜就靠它）。**绝不要给它加 `min-height:0`** —— 那会关掉 flex 项「不得小于内容高度」的默认值，侧栏空间不足时行被压扁到 21px，标题折成两行后向下溢出约 12px、压住下一条。侧栏卡片里的紧凑列表请用 `.rec-list`（见下） |
| 侧栏要放「标题 + 价格」的紧凑列表 | 用 `.rec-list`（`style.css` 里 `.rank-stat` 下面）。它按内容自然高度排、标题最多两行、价格右对齐截断。**不要复用 `.rank-list`** —— 那套的等分高度是为首页排行榜设计的 |
| 发布页城市下拉里没有我要的城市 | 选「✏️ 其他城市（自行填写）」，填过的城市会自动存进下拉（localStorage `hh_custom_cities`） |
| 发布时提示必填 | 现在只有**标题**和**城市**是必填，其余（详细描述、图片、滚动推广）都可留空 |
| 自己发的帖子删不掉 | 清 localStorage 的 `hh_posts`（F12 → Application → Local Storage） |
| 广告条看不到了 | `sessionStorage` 的 `hh_adbar_off`，关掉标签页重开即可 |
| 广告位传了图，别人看不到 | 上传的图存在**你本机浏览器**里（`hh_ad_slots`）。要让所有访客看到，点编辑器「⬇ 下载」存图到 `assets/img/`，再把「导出配置」的代码粘回 `data.js` |
| 广告位传图报「存储空间不足」 | `localStorage` 约 5MB 上限，内嵌 base64 图很占地方。把图导出成文件、`img` 改写成 `assets/img/xxx.jpg` 路径即可 |
| 广告位想恢复成「招租中」 | 编辑器里点「清除此位」，或把标题与图片都清空再保存 |
| 广告位「整幅铺满」点了没反应 | 没有图片时这个开关是灰的 —— 先上传图片或粘贴图片地址 |
| 广告位图片不显示，格子变成浅斜纹底 | 图片地址写错了（路径不对 / 文件没放进 `assets/img/` / 外链失效）。格子右上角会显示红色「广告 · 图未加载」，鼠标悬停能看到具体地址。改对 `img` 即可 |
| 广告位设了有效期，到期后管理员还能看到 | **这是设计如此**：过期后普通访客当空位看，但管理员仍能看到灰显状态 + 「已过期」角标，方便你知道该续费/撤下。每天首次加载时系统会自动物理删除过期记录 |
| 广告位到期后没自动删 | 清理每天**只执行一次**（以 `hh_ad_clean_ts` 为标记）。如果当天已经打开过首页，需等第二天或手动清掉 `hh_ad_clean_ts` 再刷新 |
| 广告位操作日志在哪看 | 存在 `localStorage` 的 `hh_ad_log` 键里（最多保留 200 条）。目前没有在页面上做可视化入口，需要时 F12 → Application → Local Storage 查看，或写个小脚本读取 |
| 上传的图想给所有人看 | 图片只存在本机浏览器。点编辑器「⬇ 下载」→ 存进 `assets/img/`（该文件夹里有 `README.txt` 说明）→ 把「图片地址」改成 `assets/img/文件名.jpg` → 保存 → 导出配置 → 粘回 `data.js` |
| 发布信息时弹「发布失败：浏览器存储空间不够」 | 浏览器本地存储约 5MB，被图片占满了。**这条信息没有存进去**（不会再假装发布成功）。按弹窗里的建议做：图片减到 1~2 张 / 换小图，或 F12 → Application → Local Storage 删掉 `hh_posts` 清旧数据 |
| 发布页选的图片会不会太大 | 不会。选图后会自动压缩到 1200px、单张 240KB 以内（和广告位同一套压缩逻辑）。上传区下面会实时显示「已选 N 张 · 共约 XX KB」，超过 1.2MB 会变成橙色提醒 |
| 弹窗怎么关 | 点「取消 / 知道了」、点弹窗外的灰色区域、或按 **Esc**（多层弹窗时只关最上面那一层） |
| 点赞 / 收藏后刷新又没了 | 说明**当时就没存进去**（存储满了），点击时会弹「保存失败：浏览器存储空间不够」，按钮状态也不会变。清理旧数据后重试 |
| 签到提示「签到失败」 | 积分没真正入账时会明确报失败，并且**不会**记成「今天已签到」，清出空间后可以重签 |
| 留言 / 充值 / 删除提示失败 | 同上，都是存储写不进去。写不进去时**不会再提示成功**，输入框内容也会保留着不让你白写 |
| 登录成功后刷新又变回未登录 | 登录态没写进去（存储满），登录时会弹「登录失败：浏览器存储空间不够」。**不会**再出现「提示登录成功 → 跳过去还是未登录 → 被弹回登录页」的打转 |
| 注册时提示「该邮箱已注册」| 同一邮箱不能重复注册。系统会检查 `hh_users` 表，已存在的邮箱请直接登录 |
| 忘记密码怎么找回 | 登录页点「找回密码」标签 → 输入注册邮箱 → 获取验证码 → 验证通过后可查看昵称并重置密码 |
| 验证码收不到 | 演示站点不会真实发送邮件，点击「获取验证码」后控制台会打印验证码（F12 → Console），或输入任意 6 位数字也可通过 |
| 注册后登录提示「该邮箱尚未注册」| 注册时存储写失败（空间满），用户没真正存进去。需清理空间后重新注册 |
| 点「全部接受 Cookie」但下次进站还弹 | 同意记录没写进去，会提示「保存失败：…你的 Cookie 选择没能记下来」。此时偏好类功能（收藏 / 草稿 / 城市）也仍然是被拒绝状态 —— 腾出空间后重新选一次即可 |
| 点「演示账号」提示「切换失败」 | 同上，登录态没写进去，此时**不会刷新页面**（避免刷新回来还是未登录，看着像按钮坏了） |
| 邀请好友后积分没加、记录里也没有 | 邀请记录和积分是**一起落的**：任一写失败就整体回退，提示「保存失败：…邀请记录和积分都没写进去」。不会出现「有记录没积分」或「有积分没记录」的假账，腾空间后可以重试 |
| 草稿提示「⚠️ 草稿没保存成功」 | 草稿写不进去（存储满）。别急着关页面，先按下面「关于存储空间不够」腾地方，再手动点一次保存 |
| 保存资料时提示「昵称已保存，但常驻城市没写进去」 | 两步写入里城市这步失败了。昵称已经存下，腾出空间后单独再改一次城市即可（不会出现「一半新一半旧」） |
| 切换城市后刷新又回到原城市 | 城市偏好没写进去，切换时会提示「保存失败：…城市没切换成功」，此时界面上方**不会**改显示新城市名 |

### 关于「存储空间不够」

本站没有后端，你的登录、发布、收藏、积分、广告位全部存在浏览器的 `localStorage` 里，**总共只有约 5MB**。
图片是最大的占用方（一张图转成文字大概会膨胀 1/3），所以：

- 图片都已经**自动压缩**过（发布页 240KB/张、广告位 380KB/张），正常用不会满；
- 一旦真的写满，**所有会写入的操作都会明确报错**，不会出现「提示成功、刷新后没了」的情况。
  统一的说法是「**保存失败：浏览器存储空间不够**」，后面跟一句具体是哪儿没存下：
  `登录失败：…登录状态没保存下来` / `发布失败：…` / `签到失败：…` / `留言没保存成功：…` /
  `删除失败：…` / `充值失败：…` / `保存失败：…你的 Cookie 选择没能记下来` /
  `保存失败：…邀请记录和积分都没写进去` / `保存失败：…城市没切换成功` /
  `昵称已保存，但常驻城市没写进去` / `⚠️ 草稿没保存成功`；
- **写入成对的操作会一起回退**，不留半截数据：签到（标记 + 积分）、邀请好友（记录 + 积分）、
  保存资料（昵称 + 城市）。失败后腾出空间就能原样重试；
- 想腾地方：F12 → Application → Local Storage，删掉 `hh_posts`（自己发过的信息）或 `hh_ad_slots`（广告位图片），
  或者把图片按 5.5 节的办法改成 `assets/img/` 路径（**这是最省空间的做法**，路径只占几十个字符）。

### 写操作的返回约定（改代码时看这里）

底层 `LS.set(key, val)` 在**存储写不进去**或**该 Cookie 类别未授权**时会返回 `false`（不会抛错）。
所以上层每个写操作都必须**把真实结果往上传**，界面才能说实话。现有约定：

| 返回值 | 含义 | 例子 |
|---|---|---|
| `true` / `false` | 确实存下了 / 没存下 | `addMyPost()`、`addComment()`、`saveDraft()`、`setCity()`、`setAdSlot()`、`clearAdSlot()`、`setPoints()`、`setUser()`、`addView()` |
| `true` / `false` / `null` | 已收藏 / 已取消 / **没保存成功** | `toggleFav()`、`toggleLike()` |
| 数字 | **真实余额**（写失败时返回没变的那个数，不是「打算写入的数」） | `addPoints()` |
| `'ok'` / `'done'` / `'fail'` | 成功 / 今天已签过 / 存储写不进去 | `signIn()` |
| `{ ok, gained, points }` | 记录是否存下 / 实际到账积分 / 变动后余额 | `addInvite()` |
| 同意记录 / `null` | 存下了 / 没存下（选择未生效） | `Cookie.choose()`、`applyCookieChoice()` |

两条额外规则：

1. **成对的写入要一起回退**。例如签到是「先写 `hh_sign_date` 再发积分」，积分没入账就把标记清掉，
   否则用户会「今天签过了但没拿到分」；邀请好友同理（记录 + 积分）。
2. **失败时不要改界面状态**。城市切换失败就别改标题栏的城市名，登录态写失败就别跳转 ——
   界面显示的东西必须和存储里的一致。

新增任何写操作时，请照上面的约定把 `LS.set(...)` 的返回值 `return` 出去，并在调用处补失败分支。

---


## 10. 想更省事？让 AI 帮你改

直接说需求即可，例如：

- 「把主色调改成墨绿色 `#2F5D50`」
- 「在租房分类里加 10 条鹿特丹的房源」
- 「滚动广告条换成 6 条装修和家政的内容」
- 「加一个『二手交易』分类，子分类是家具/电器/车辆/母婴」

AI 会直接定位到对应行号改好，你只需刷新看效果。
