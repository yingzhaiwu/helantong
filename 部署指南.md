# 荷兰同城（NTC）部署指南

## 一、域名与DNS配置

### 1.1 域名注册
推荐注册商：
- **Namecheap**（国际）
- **阿里云**（国内）
- **Cloudflare**（推荐，自带CDN）

### 1.2 DNS解析配置
```
Type    Name    Value                TTL
A       @       你的服务器IP          600
A       www     你的服务器IP          600
CAA     @       0 issue "letsencrypt.org"  86400
```

**验证命令：**
```bash
# 检查A记录
nslookup ntc.nl
# 检查CAA记录
dig CAA ntc.nl
```

## 二、服务器选型

### 2.1 推荐方案
| 方案 | 提供商 | 配置 | 月费 | 适用场景 |
|------|--------|------|------|----------|
| **VPS** | Vultr / DigitalOcean | 1核1GB | $5 | 起步/测试 |
| **VPS** | Linode / Hetzner | 2核4GB | $10-20 | 生产环境 |
| **云服务器** | 阿里云ECS | 2核4GB | ¥200+ | 国内访问 |
| **静态托管** | Cloudflare Pages | - | 免费 | 纯静态站点 |

### 2.2 系统要求
- **OS**: Ubuntu 22.04 LTS / Debian 12
- **Web服务器**: Nginx（推荐）或 Apache
- **SSL**: Certbot + Let's Encrypt

## 三、环境配置

### 3.1 系统更新
```bash
# 登录服务器
ssh root@你的服务器IP

# 更新系统
apt update && apt upgrade -y

# 安装必要工具
apt install -y curl wget vim ufw fail2ban
```

### 3.2 安装Nginx
```bash
# 安装Nginx
apt install -y nginx

# 启动并设置开机自启
systemctl start nginx
systemctl enable nginx

# 验证安装
curl -I http://localhost
# 应返回 HTTP/1.1 200 OK
```

### 3.3 配置防火墙
```bash
# 允许SSH、HTTP、HTTPS
ufw allow OpenSSH
ufw allow 'Nginx Full'
ufw enable

# 验证状态
ufw status
```

## 四、网站部署

### 4.1 上传网站文件
```bash
# 创建网站目录
mkdir -p /var/www/ntc

# 设置权限
chown -R www-data:www-data /var/www/ntc
chmod -R 755 /var/www/ntc

# 上传文件（本地执行）
scp -r /path/to/helan-tongcheng/* root@你的服务器IP:/var/www/ntc/

# 或使用rsync
rsync -avz --delete /path/to/helan-tongcheng/ root@你的服务器IP:/var/www/ntc/
```

### 4.2 Nginx配置
创建配置文件 `/etc/nginx/sites-available/ntc`：

```nginx
server {
    listen 80;
    listen [::]:80;
    server_name ntc.nl www.ntc.nl;
    root /var/www/ntc;
    index index.html;

    # 启用gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # 缓存静态资源
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 6M;
        access_log off;
        add_header Cache-Control "public, immutable";
    }

    # HTML文件不缓存（确保内容更新及时）
    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
    }

    # 安全响应头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # 404页面
    error_page 404 /index.html;

    # 日志
    access_log /var/log/nginx/ntc-access.log;
    error_log /var/log/nginx/ntc-error.log;
}
```

启用配置：
```bash
# 创建符号链接
ln -s /etc/nginx/sites-available/ntc /etc/nginx/sites-enabled/

# 删除默认配置
rm /etc/nginx/sites-enabled/default

# 测试配置
nginx -t

# 重载Nginx
systemctl reload nginx
```

## 五、SSL证书配置

### 5.1 安装Certbot
```bash
# 安装Certbot
apt install -y certbot python3-certbot-nginx

# 获取并安装证书
certbot --nginx -d ntc.nl -d www.ntc.nl

# 按提示操作：
# 1. 输入邮箱
# 2. 同意服务条款
# 3. 选择是否接收邮件（可选）
# 4. 选择是否强制HTTPS（推荐选择2：Redirect）
```

### 5.2 验证SSL
```bash
# 检查证书
certbot certificates

# 测试HTTPS访问
curl -I https://ntc.nl
# 应返回 HTTP/2 200 OK

# SSL测试（在线）
# 访问 https://www.ssllabs.com/ssltest/ 输入域名测试
```

### 5.3 自动续期
```bash
# 测试自动续期
certbot renew --dry-run

# 设置定时任务（通常Certbot会自动设置）
systemctl status certbot.timer
```

## 六、性能优化

### 6.1 启用HTTP/2
```bash
# 编辑Nginx配置
vim /etc/nginx/sites-available/ntc

# 确保listen行包含http2
# listen 443 ssl http2;
# listen [::]:443 ssl http2;

# 重载Nginx
systemctl reload nginx
```

### 6.2 启用Brotli压缩（可选，比gzip更高效）
```bash
# 安装Brotli模块
apt install -y libnginx-mod-brotli

# 在nginx.conf中添加
load_module modules/ngx_http_brotli_filter_module.so;
load_module modules/ngx_http_brotli_static_module.so;

# 在server块中添加
brotli on;
brotli_comp_level 6;
brotli_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
```

### 6.3 图片优化
```bash
# 安装图片优化工具
apt install -y jpegoptim pngquant

# 批量优化图片（本地执行）
find /var/www/ntc/assets/img -name "*.jpg" -exec jpegoptim --strip-all --max=80 {} \;
find /var/www/ntc/assets/img -name "*.png" -exec pngquant --quality=80-95 --force --output {} {} \;
```

## 七、监控与日志

### 7.1 配置日志轮转
```bash
# 编辑日志轮转配置
cat > /etc/logrotate.d/nginx-ntc << 'EOF'
/var/log/nginx/ntc-*.log {
    daily
    missingok
    rotate 14
    compress
    delaycompress
    notifempty
    create 0640 www-data adm
    sharedscripts
    postrotate
        [ -f /var/run/nginx.pid ] && kill -USR1 `cat /var/run/nginx.pid`
    endscript
}
EOF
```

### 7.2 基础监控脚本
```bash
# 创建监控脚本
cat > /usr/local/bin/ntc-monitor.sh << 'EOF'
#!/bin/bash
# 检查Nginx状态
if ! systemctl is-active --quiet nginx; then
    echo "$(date): Nginx is down, restarting..." >> /var/log/ntc-monitor.log
    systemctl restart nginx
fi

# 检查SSL证书过期时间（30天内警告）
EXPIRY=$(echo | openssl s_client -servername ntc.nl -connect ntc.nl:443 2>/dev/null | openssl x509 -noout -dates | grep notAfter | cut -d= -f2)
EXPIRY_EPOCH=$(date -d "$EXPIRY" +%s)
NOW_EPOCH=$(date +%s)
DAYS_LEFT=$(( (EXPIRY_EPOCH - NOW_EPOCH) / 86400 ))

if [ $DAYS_LEFT -lt 30 ]; then
    echo "$(date): SSL certificate expires in $DAYS_LEFT days" >> /var/log/ntc-monitor.log
fi
EOF

chmod +x /usr/local/bin/ntc-monitor.sh

# 添加定时任务（每5分钟检查一次）
echo "*/5 * * * * root /usr/local/bin/ntc-monitor.sh" >> /etc/crontab
```

## 八、SEO验证

### 8.1 Google Search Console
1. 访问 https://search.google.com/search-console
2. 添加属性：`ntc.nl`
3. 验证方式：HTML文件上传或DNS记录
4. 提交Sitemap：`https://ntc.nl/sitemap.xml`

### 8.2 验证工具
```bash
# 检查robots.txt
curl https://ntc.nl/robots.txt

# 检查sitemap
curl https://ntc.nl/sitemap.xml

# 检查响应头
curl -I https://ntc.nl/

# 检查HTTPS重定向
curl -I http://ntc.nl/
# 应返回 301 Moved Permanently 到 https://
```

### 8.3 性能测试
- **PageSpeed Insights**: https://pagespeed.web.dev/
- **GTmetrix**: https://gtmetrix.com/
- **WebPageTest**: https://www.webpagetest.org/

## 九、备份策略

### 9.1 自动备份脚本
```bash
# 创建备份脚本
cat > /usr/local/bin/ntc-backup.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/backup/ntc"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR

# 备份网站文件
tar -czf $BACKUP_DIR/ntc_files_$DATE.tar.gz -C /var/www ntc

# 备份Nginx配置
tar -czf $BACKUP_DIR/ntc_nginx_$DATE.tar.gz -C /etc/nginx sites-available sites-enabled nginx.conf

# 备份SSL证书
tar -czf $BACKUP_DIR/ntc_ssl_$DATE.tar.gz -C /etc/letsencrypt live

# 保留最近30天的备份
find $BACKUP_DIR -name "*.tar.gz" -mtime +30 -delete

echo "$(date): Backup completed" >> /var/log/ntc-backup.log
EOF

chmod +x /usr/local/bin/ntc-backup.sh

# 每天凌晨3点备份
echo "0 3 * * * root /usr/local/bin/ntc-backup.sh" >> /etc/crontab
```

## 十、故障排查

### 10.1 常见问题

**Nginx无法启动：**
```bash
# 检查配置语法
nginx -t

# 查看错误日志
tail -f /var/log/nginx/error.log

# 检查端口占用
ss -tlnp | grep :80
```

**SSL证书问题：**
```bash
# 检查证书状态
certbot certificates

# 强制续期
certbot renew --force-renewal

# 检查证书链完整性
openssl s_client -connect ntc.nl:443 -servername ntc.nl
```

**网站访问慢：**
```bash
# 检查服务器负载
uptime

# 检查内存使用
free -h

# 检查磁盘空间
df -h

# 检查Nginx连接数
ss -ant | grep :443 | wc -l
```

### 10.2 紧急恢复
```bash
# 如果配置错误导致Nginx无法启动
mv /etc/nginx/sites-enabled/ntc /tmp/
ln -s /etc/nginx/sites-available/default /etc/nginx/sites-enabled/
systemctl restart nginx

# 从备份恢复
tar -xzf /backup/ntc/ntc_files_20260920_030000.tar.gz -C /var/www/
```

---

**部署完成验证清单：**
- [ ] 域名解析正确（`nslookup ntc.nl`）
- [ ] HTTP访问正常（`curl http://ntc.nl`）
- [ ] HTTPS访问正常（`curl https://ntc.nl`）
- [ ] HTTP自动跳转HTTPS（`curl -I http://ntc.nl` 返回301）
- [ ] SSL证书有效（浏览器无警告）
- [ ] robots.txt可访问（`curl https://ntc.nl/robots.txt`）
- [ ] sitemap.xml可访问（`curl https://ntc.nl/sitemap.xml`）
- [ ] 响应头包含安全头（X-Frame-Options等）
- [ ] gzip压缩生效（`curl -I -H "Accept-Encoding: gzip" https://ntc.nl`）
- [ ] Google Search Console已验证
- [ ] 备份脚本已配置并测试
